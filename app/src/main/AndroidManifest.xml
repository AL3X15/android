<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.smartcity">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat">
        <activity android:name=".activity.PasswordActivity"></activity>
        <activity android:name=".activity.PostulationActivity" />
        <activity android:name=".activity.ResultatActivity" />
        <activity android:name=".activity.EditProfilActivity" />
        <activity android:name=".activity.FaqActivity" />
        <activity
            android:name=".activity.RechercheAnnonceActivity"
            android:screenOrientation="portrait" />
        <activity android:name=".activity.EmploiDuTempsActivity" />
        <activity android:name=".activity.DetailAnnonceActivity" />
        <activity android:name=".activity.AcceuilActivity" />
        <activity android:name=".activity.InscriptionActivity" />
        <activity android:name=".activity.ConnexionActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>